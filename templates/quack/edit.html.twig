{% extends 'base.html.twig' %}

{% block title %}Edit Quack{% endblock %}

{% block body %}
    <div class="container my-5">
        <h1 class="text-center mb-4">Edit Quack</h1>

        <div class="card shadow-sm">
            <div class="card-body">
                {{ include('quack/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_quack_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>

            {{ include('quack/_delete_form.html.twig', {'class': 'd-inline'}) }}
        </div>
    </div>
{% endblock %}
