{% extends 'base.html.twig' %}

{% block title %}Ducks index{% endblock %}

{% block body %}
    <h1>Ducks index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for duck in ducks %}
            <tr>
                <td>{{ duck.id }}</td>
                <td>{{ duck.email }}</td>
                <td>{{ duck.roles ? duck.roles|json_encode : '' }}</td>
                <td>{{ duck.password }}</td>
                <td>
                    <a href="{{ path('app_ducks_show', {'id': duck.id}) }}">show</a>
                    <a href="{{ path('app_ducks_edit', {'id': duck.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ducks_new') }}">Create new</a>
{% endblock %}
